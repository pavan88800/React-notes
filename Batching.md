Batching is about React combining multiple state updates into a single render for performance.

Example: calling setState multiple times inside an event handler will usually only trigger one re-render.
