# 🧠 Screen Reader & Keyboard Accessibility (A11y) – Frontend Notes

> **Goal:** Build UIs that work for keyboard users and screen-reader users **by default**, with minimal ARIA and fewer bugs.

---

## 🔑 Golden Rule (Never Forget)

> **Screen readers rely on keyboard navigation and semantic HTML.**
> If it works well with a keyboard, it usually works well with screen readers.

---

## ✅ Always Prefer Semantic HTML (Native First)

### 1️⃣ `<button>` – For actions

Use when something **does something** (submit, toggle, open, close).

**Built-in benefits:**

- Focusable with `Tab`
- Activated with `Enter` / `Space`
- Screen reader announces: **"Button"**

```jsx
<button onClick={handleClick}>Save</button>
```

---

### 2️⃣ `<a href="">` – For navigation

Use when something **navigates to another page/route**.

```jsx
<a href="/profile">Go to Profile</a>
```

> ❌ Don’t use `<button>` for navigation

---

### 3️⃣ Form elements – For user input

Use native inputs for forms:

- `<input>`
- `<textarea>`
- `<select>`

They come with:

- Keyboard support
- Screen-reader support
- Proper roles automatically

---

### 4️⃣ `<label>` – Mandatory for inputs

```html
<label for="email">Email</label> <input id="email" />
```

Screen reader announces:

> "Email, edit text"

---

## 🚫 Common Anti-Patterns (Avoid These)

### ❌ Clickable `<div>` / `<span>`

```jsx
<div onClick={submit}>Submit</div>
```

**Why it’s bad:**

- Not focusable by default
- Not announced as interactive
- Keyboard & screen-reader users are blocked

---

### ❌ Overusing `tabIndex`

If you are adding `tabIndex={0}` everywhere → **something is wrong**.

Use semantic elements instead of forcing focus manually.

---

### ❌ Using ARIA when HTML already solves it

```jsx
<div role="button">Save</div> // ❌
<button>Save</button>          // ✅
```

> **Rule:** Native HTML > ARIA

---

## ⌨️ Keyboard Interaction Rules (Must Know)

| Key           | Behavior                             |
| ------------- | ------------------------------------ |
| `Tab`         | Move focus forward                   |
| `Shift + Tab` | Move focus backward                  |
| `Enter`       | Activate buttons & links             |
| `Space`       | Activate buttons / toggle checkboxes |
| `Arrow Keys`  | Used in menus, grids, carousels      |

---

## 🧩 UI Pattern → Correct Element

| UI Component      | Use This                  |
| ----------------- | ------------------------- |
| Button / CTA      | `<button>`                |
| Navigation        | `<a>`                     |
| Accordion header  | `<button>`                |
| Modal close icon  | `<button>`                |
| Checkbox          | `<input type="checkbox">` |
| Tabs              | `<button>` + ARIA         |
| Carousel controls | `<button>`                |

---

## 🧠 Screen Reader Mental Model

Screen readers ask only **three questions**:

1. **Can I focus it?**
2. **Does it say what it is?**
3. **Can I activate it with the keyboard?**

If all three are **YES** → accessibility is good ✅

---

## 🔥 Interview-Ready One-Liners

- "I prefer semantic HTML because it provides built-in keyboard and screen-reader accessibility."
- "Native elements reduce the need for ARIA and custom key handling."
- "If it’s clickable, it should be a `<button>`."

---

## 🏁 Final Rule for Life

> **Clickable → `<button>`** > **Navigation → `<a>`** > **Input → `<input>`**

---

✅ If you follow this README, you will **never break screen-reader accessibility accidentally**.
